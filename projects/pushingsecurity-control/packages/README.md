# Packages

Shared code belongs here, not inside a random app.

Use these lanes:

- `ui/` for shared components and design primitives
- `integrations/` for provider SDK wrappers and service bridges
- `schemas/` for shared contracts and validation models
- `config/` for shared tooling configuration
